# 説明書 — VBA_Tools

## 一言でいうと

Excel（エクセル）の繰り返し作業を自動で行う、小さな道具（マクロ）を集めたものです。

## 何の役に立つのか

Excel を使う仕事では、同じ操作の繰り返しに時間を取られがちです。
たとえば、次のような作業です。

- 何十個ものファイルの中身を、1つの表にまとめ直す
- フォルダの中にどんなファイルがあるか、一覧表を作る
- 資料の中の特定の言葉だけに、色を付けて目立たせる

この道具集を使うと、こうした作業を Excel が自動で行います。
手作業より速く、写し間違いも起きません。

## 使い方のおおまかな流れ

1. 使いたい道具のファイル（名前の末尾が .bas のもの）をダウンロードします。
2. Excel を開き、キーボードの Alt キーと F11 キーを同時に押します。「VBE」という編集画面が開きます。
3. 画面上部の「ファイル」から「ファイルのインポート」を選び、ダウンロードしたファイルを読み込みます。
4. Excel の画面に戻り、Alt キーと F8 キーを同時に押し、実行したいマクロを選んで「実行」を押します。

日本語が文字化けする場合など、うまくいかないときは README の「注意事項」をご覧ください。

## ファイルの案内

| ファイル名 | できること |
|---|---|
| Excelファイルを結合.bas | 複数のフォルダに散らばった Excel ファイルから、決まったシートのデータを1つのブックに集めます |
| フォルダ階層ファイル一覧作成.bas | フォルダの中のファイルを種類別（Excel・テキスト・CSV）に整理した一覧表を作ります |
| ファイルパス分解処理.bas | ファイルの置き場所（パス）を、フォルダ名やファイル名に分解して表にします |
| ファイル最終更新日取得.bas | 表に書いた各ファイルについて、最後に更新された日時を調べて隣の列に書き出します |
| エクセルテキスト取得.bas | Excel や PowerPoint のファイルから、文字だけを抜き出して一覧にします |
| 全量取得_コード_シートパス.bas | 表に書いたマクロファイルの中身を、1行ずつ Excel に書き出します（点検・棚卸し用） |
| 全量取得_コード_フォルダ指定.bas | 指定したフォルダにあるマクロファイルの中身を、1行ずつ Excel に書き出します |
| マクロキー情報取得.bas | ブックの中のマクロに割り当てられたショートカットキーを一覧にします |
| 特定文字色変更.bas | シートの中の指定した言葉に、色や太字を付けます |
| 色_文字列.bas | 特定文字色変更.bas と同じ働きを、別の書き方で実現したものです |
| 拡大率変更.bas | ブック内の全シートの表示の大きさ（拡大率）を一度にそろえます |
| 列指定下埋.bas | 指定した列の空白セルを、すぐ上のセルの値で埋めます |
| 目次作成_相対パス.bas | ブック内の全シートへ飛べる目次シートを自動で作ります |
| 業務効率化マクロ集.bas | 行や列の挿入・削除、幅や高さの自動調整など、細かな時短操作を集めたものです |

## 用語の小辞典

| 用語 | 意味 |
|---|---|
| マクロ | Excel に一連の操作を自動で行わせる仕組みのことです |
| VBA | マクロを作るためのプログラミング言語の名前です |
| VBE | マクロを書いたり読み込んだりするための編集画面です（Alt + F11 で開きます） |
| .bas ファイル | マクロの中身が入ったテキストファイルです。VBE から読み込んで使います |
| ブック | Excel のファイル1つのことです。中に複数のシートを持てます |
| シート | Excel の1枚1枚の表のことです。画面下のタブで切り替えます |
